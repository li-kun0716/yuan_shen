import {

    create,
    NConfigProvider,
    NMessageProvider,
    NButton,
    NIcon,
    NCollapse,
    NCollapseItem,
    NCarousel,
    NPagination,
    NSwitch,
    NDrawer,
    NDrawerContent,
    NProgress,
    NSlider,
    NCarouselItem
} from 'naive-ui'

const naive = create({
    components: [
        NConfigProvider,
        NMessageProvider,
        NButton,
        NIcon,
        NCollapse,
        NCollapseItem,
        NCarousel,
        NPagination,
        NSwitch,
        NDrawer,
        NDrawerContent,
        NProgress,
        NSlider,
        NCarouselItem

    ],
})

export default naive
